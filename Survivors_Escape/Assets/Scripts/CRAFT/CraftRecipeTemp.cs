using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class CraftRecipeTemp : MonoBehaviour
{
    public Image reicon;
    public TextMeshProUGUI renm;
    public TextMeshProUGUI reqr;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
